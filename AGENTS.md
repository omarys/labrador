# Agent Guidelines

## Agent skills

### Issue tracker

Tracked in GitHub Issues (`omarys/labrador`). See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical 5-label triage workflow (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context (`CONTEXT.md` and `docs/adr/`). See `docs/agents/domain.md`.
